# human-activity-clustering
Unsupervised clustering of human activities from smartphone sensor signals. Includes feature extraction, dimensionality reduction (PCA, Kernel PCA, t‑SNE) and comparison of clustering methods (K‑Means, Hierarchical, Spectral).
